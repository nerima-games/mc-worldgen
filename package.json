{
  "name": "@nerima-games/mc-worldgen",
  "version": "0.1.0",
  "type": "module",
  "description": "World generation for the nerima-games Minecraft-clone rebuild: biome classification, deterministic terrain, carvers, vegetation, structures, chunk lifecycle and the light grid. No renderer, ever.",
  "homepage": "https://github.com/nerima-games/mc-worldgen#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nerima-games/mc-worldgen.git"
  },
  "bugs": {
    "url": "https://github.com/nerima-games/mc-worldgen/issues"
  },
  "author": "takeokunn",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.15.0"
  },
  "packageManager": "pnpm@9.15.0",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "files": [
    "index.ts",
    "domain",
    "docs",
    "tsconfig.base.json",
    "LICENSE",
    "README.md"
  ],
  "keywords": [
    "minecraft",
    "effect-ts",
    "typescript",
    "voxel",
    "worldgen",
    "terrain",
    "biome"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.build.json --pretty false && tsc -p tsconfig.test.json --pretty false",
    "lint": "oxlint index.ts domain scripts test",
    "lint:fix": "oxlint --fix index.ts domain scripts test",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check:deps": "tsx scripts/check-dependency-whitelist.ts",
    "verify": "pnpm typecheck && pnpm lint && pnpm check:deps && pnpm test"
  },
  "dependencies": {
    "effect": "^3.20.0"
  },
  "devDependencies": {
    "@effect/vitest": "^0.25.1",
    "@types/node": "^22.19.11",
    "@vitest/coverage-v8": "^3.2.4",
    "oxlint": "^0.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
